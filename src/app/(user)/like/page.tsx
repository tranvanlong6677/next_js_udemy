import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Your liked",
  description: "Liked has list track you like",
};

const LikePage = () => {
  return <>Like page</>;
};

export default LikePage;
